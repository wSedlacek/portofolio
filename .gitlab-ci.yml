image: node:8.12.0

pages:
  cache:
    paths:
      - node_modules/

  stage: deploy
  script:
    - yarn global add @angular/cli@6.2.1
    - yarn
    - yarn build
    - yarn deploy:gitlab
  artifacts:
    paths:
      - public
  only:
    - master
    - pages
